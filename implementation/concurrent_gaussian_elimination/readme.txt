Aby uruchomić program, z katalogu 'concurrent_gaussian_elimination' należy wywołać następujące polecenie:

./gradlew run --args='inputPath outputPath'

inputPath - ścieżka do pliku wejściowego
outputPath - ściezka do pliku wyjściowego